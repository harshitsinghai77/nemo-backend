version: "3"
services:
  postgresql:
    image: "postgres:13"
    environment:
      POSTGRES_DB: "nemo"
      POSTGRES_USER: "nemo"
      POSTGRES_PASSWORD: "password"
    ports:
      - "5432:5432"
